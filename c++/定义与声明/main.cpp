#include <iostream>

// 定义与声明
/*
  
  函数的声明
  int test(int, int);

  函数的定义
  int test(int a, int b){ return a + b };

  注意：函数默认都会被识别为声明(函数默认都会添加 extern)

  变量的声明
  extern int a;

  变量的定义
  int a;  int b = 100;

  注意：变量默认都会被识别为定义，那变量的声明其实是为了全局变量

  编译器角度理解定义与声明
  未运行之前，你只要声明，运行的时需要定义
*/



int main()
{
  
  return 0;
}
