#include <stdio.h>
#include <stdlib.h>
#include <string.h>

// 内存对齐
// 以空间换时间
// 1.没有内存对齐
/*
  char char int => 计算机一个字节一个字节读取，
  int型 不就得读取4次，然后在做一个合并操作？
  所以内存读取的时候不是读取字节，是块。块是可以定义的
  2 4 8 16等
*/

void test01(void) {

}

int main(void)
{
  return 0;
}
